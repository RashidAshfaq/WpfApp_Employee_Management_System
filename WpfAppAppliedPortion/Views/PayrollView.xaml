<UserControl x:Class="WpfAppAppliedPortion.Views.PayrollView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             d:DesignHeight="450" d:DesignWidth="800">
	<Grid>
		<DataGrid Name="PayrollDataGrid" AutoGenerateColumns="True" />
		<StackPanel Orientation="Horizontal" HorizontalAlignment="Center" VerticalAlignment="Bottom">
			<Button Name="AddButton" Content="Add" Click="AddButton_Click" />
			<Button Name="UpdateButton" Content="Update" Click="UpdateButton_Click" />
			<Button Name="DeleteButton" Content="Delete" Click="DeleteButton_Click" />
			<Button Name="RefreshButton" Content="Refresh" Click="RefreshButton_Click" />
		</StackPanel>
	</Grid>
</UserControl>
